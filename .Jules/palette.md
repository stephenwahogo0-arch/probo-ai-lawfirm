# Palette's Journal

## 2026-05-18 - Form Accessibility Foundations
**Learning:** In this codebase, many forms use `<label>` elements for visual titling but lack `htmlFor` associations with their corresponding inputs, which breaks screen reader support.
**Action:** Always ensure `htmlFor` on labels and `id` on inputs are matched, especially in core intake forms.
